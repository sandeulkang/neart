import 'package:flutter/material.dart';

class regionExhibit extends StatefulWidget {
  const regionExhibit({Key? key}) : super(key: key);

  @override
  State<regionExhibit> createState() => _regionExhibitState();
}

class _regionExhibitState extends State<regionExhibit> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

